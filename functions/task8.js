// Task 8: Create a function that takes in a number as an argument and returns the result of finding the square root of that number.

function sqrRoot(num) {
    return Math.sqrt(num);
}

console.log(sqrRoot(25));